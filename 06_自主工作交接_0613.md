# 自主工作交接(2026-06-13 凌晨批次)

> 你休息期间完成的工作与回来后需要做的事。全部改动未触碰已投产链路的正确性(新模块未接线,改动均有测试)。

## 已完成

### 1. 任期数据补齐 ★(解锁 N4/N5)

- 用且慢 MCP 批量拉取候选池 **632 只全部命中**,产出 `scripts/data/manager_tenure.csv`(已校验 md5)
- 字段:最长/最短任期、近 6 月是否新增经理、经理名单、且慢口径规模
- **已接入 run_monthly**(有文件自动合并,无文件跳过)
- 发现:候选池中 **72 只最长任期 <1 年(应触发 N4)、44 只近半年新增经理(N5 信号)**——首跑全部漏放,下次跑批初筛会明显收紧

### 2. 基准解析器升级(benchmark.py 重写)

- 修复三类解析失败:权重在前("65%×指数")、全角＋、债券指数别名(中证全债/中债综合系→中债综合代理)
- **新安全规则**:已映射权重之和 <50% 时强制回退默认基准——杜绝"医药80%+国债20%只匹配到国债"的失真基准
- 映射表扩到 30+ 条目(含中证红利/医药卫生/内地消费/新能源/上证A股等)
- 实败样本回归测试通过;test_engine 新增 3 条断言
- 预期效果:基准解析率从 60% 显著提升

### 3. classify.py 同类组细分模块(新文件,**未接线**)

- 资产主干(普通股票/偏股混合/灵活配置)× 行业主题单列(13 类关键词)× 策略标签(量化/沪港深/红利等)
- 灵活配置 <60% 仓位出组复核;分类置信度三档;组内 <10 只不单独评级的检查函数
- 合成数据断言通过。**等你确认设计后接入 run_monthly**(接线后评分分位将在子组内计算)

### 4. 研报提炼进度 6/17

- 第 2 批 4 篇(海通/银河/上证/济安)已入《研报方法要点》
- 亮点:海通"相对基准最大回撤"可直接借入 B 维;银河"同类≥10 只才评级"已用于 classify 设计;上证 a 值净值回归是 v0.2 固收+选券能力的方案
- 定时任务今天 12:30 自动跑第 7-8 篇

## 回来后请做(顺序)

```powershell
cd D:\03_AI_Projects_and_Vault\Fund_Evaluation\scripts
.venv\Scripts\activate
python test_engine.py          # 应显示新增的基准解析断言通过
python run_monthly.py --limit 500   # 验证: 任期补充命中数、N4/N5剔除出现、基准parsed比例提升
cd ..
.\run.ps1 sync "feat: 任期数据接入+基准解析器升级+classify模块"
```

## 待你决策(下次会话)

1. classify.py 是否按当前设计接入主流程(接入后全量重跑一次)
2. N4 触发的 72 只是否立即从候选池剔除(还是下次月度跑批自然生效)
3. 中金 FOF8 / 华宝净值 Campisi 两篇全文,要么慧博手动下,要么试豆包协作

## 已知风险与说明

- 任期 CSV 仅覆盖本期候选池 632 只,非候选池基金 N4/N5 仍缺数据(下次跑批可对新候选池增量补拉)
- 沙箱挂载同步偶发滞后(昨日曾致一次文件损坏,已重建并改为只用主机侧工具写文件)——脚本文件以 Git 仓库为准
