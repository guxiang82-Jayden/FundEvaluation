# 交接备忘 · 2026-06-13 全天

> 面向下次会话 / 其他 agent 的快速接手文档。读完此页 + `07_路线图v1.0.md` + `research/基金评估-核心原则_过去与未来.md` 即可接手。

## 一、一句话现状

主动权益广覆盖评分线 **v0.2 收口**:全市场 4926 只可完整跑通"分类→初筛→同类组细分→五维记分卡→输出四表",评分带可信度标识,小微/不可投基金已分流。综合分当前实为 A+B 两维(覆盖率~55%),已诚实标记 provisional,正式重点池暂为 0(待 v0.3 维度补全)。

## 二、今日完成(按时间)

1. **任期数据接入**:且慢 MCP 拉候选池 632 只经理任期 → `scripts/data/manager_tenure.csv` → N4(任期<1年)/N5(近期换将)规则全量生效
2. **基准解析器升级**:权重在前/全角＋/债券指数别名修复 + 残缺基准强制回退；映射表扩到 30+；解析率 52%→68%
3. **classify.py 同类组细分接入主流程**:资产主干(普通股票/偏股/灵活)× 行业主题13类单列 × 策略标签；评分改为**子组内分位**(行业基金在自己赛道比)；组内<10只回退主干组
4. **评分可信度三层防护**:
   - provisional 标识(维度覆盖率<75% → 只进候选池不进正式重点池)
   - A维(收益主维)整维缺失 → 否决,不进任何池(根治"低波动小基金虚高")
   - 可投性过滤(规模<2亿或缺失 → investability_warn)
5. **A维NaN根因修复**:合成基准与基金近3年窗口重叠不足 → 自动回退默认基准(中证800);全量层面 294 只触发,A维NaN从578→~0
6. **输出拆四表**:可投主榜 / 小微观察区 / 主题观察池 / 剔除清单(各表全量,控制台仅预览Top10)
7. **核心原则文档**:`research/基金评估-核心原则_过去与未来.md`——"过去不代表未来、越久未必越可靠、权重动态化、3-6月复盘迭代",认识论优先级高于一切指标
8. **v1.0 路线图**:`07_路线图v1.0.md`
9. **研报提炼**:第2批4篇(海通/银河/上证/济安)入库,进度6/17;豆包协作补全华宝净值Campisi全文 + 中金FOF8笔记
10. **基础设施**:Git 双端同步(NAS Gitea + GitHub,GitHub走7897代理);电源睡眠关闭防中断

## 三、当前数据/质检基线(全量4926只,2026-06-13)

- 初筛剔除1331:规模1139 + 次新197 + 任期68 + 换将44
- 子组14个;可投主榜 vs 小微观察区已分流
- 基准:parsed 2281 + fallback 970 + fallback_overlap 294
- 5y覆盖75.5%;否决532(含主维缺失);覆盖率均值48%
- 存档:`archive/score_active_equity_2026-06-13.xlsx`(及 _v3 后缀)

## 四、下一步(v0.3,路线图详述)

**建议并行启动两条**:

1. **C维归因接入**(让综合分名副其实):
   - C1 选股贡献:且慢 `getFundBrinsonIndicator`(已验证)
   - C2 风格稳定性:`rbsa.py` 已就绪 + 巨潮风格指数
   - C4 集中度:且慢 `getFundIndustryConcentration`(已验证)
   - 之后 D维(经理/容量,数据大半已有)、E维(费率/换手)
2. **review.py 复盘骨架**(核心原则落地,越早攒多期数据越好):
   - 季度频率;上期名单 vs 本期表现;命中率/RankIC/规则有效性/权重敏感性
   - 季度定时任务;输入 archive/ 历史存档

## 五、已知技术债(不阻塞,排期见路线图第八节)

- 累计净值非真复权(高分红基金低估)→ v0.2尾
- 零星 `reindex duplicate`:003984/009092/481013 等个别基金跳过 → 待排查
- 任期表仅覆盖候选池632只,非候选缺N4/N5 → 每月增量补拉
- 持有人结构(N6)、近4季仓位中枢(N9现用单期)无源
- 中金FOF8 / 华宝Campisi 的PDF原文待手动下载(链接在 `research/素材备查/00_素材清单.md`)

## 六、关键操作速查

```powershell
cd D:\03_AI_Projects_and_Vault\Fund_Evaluation
.\run.ps1 score [N]     # 评分(可选限N只);全量前先 powercfg /change standby-timeout-ac 0
.\run.ps1 test          # 引擎自检(13+项)
.\run.ps1 sync ["说明"]  # 提交+双端推送(GitHub需7897代理已配)
```

- 脚本在 `scripts/`(venv在此),git在仓库根目录
- 配置全在 `scripts/config.py`(阈值/权重/映射,调参只动这里)
- MCP仅会话内可用,定时脚本走AKShare;MCP数据用于交叉校验与深研

## 七、后台自动任务

- 研报提炼:每日12:30,`fund-report-digest`,剩9篇,完成后自停
- （待建）季度复盘:review.py + 定时任务,v0.3启动时搭骨架
