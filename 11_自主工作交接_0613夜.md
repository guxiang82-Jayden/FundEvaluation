# 自主工作交接 · 2026-06-13 夜批次

> 你休息期间完成的四项授权工作。均不改已投产逻辑、有测试/标注,回来验证后提交即可。

## 完成清单(4/4)

### 1. C 维数据继续拉取(25 → 34 只)
- 会话内用且慢 MCP 又拉 9 只主榜基金的 Brinson+集中度+换手,追加进 `scripts/data/cdim_data.csv`(现 34 只 + 表头 = 35 行)
- 已校验无重复;集中度/换手全齐,选股贡献缺 4 只(新基金无3年Brinson,正常)
- 剩余主榜约 1960 只待拉 → 明天 macOS agent 子任务继续(去重已有 34 只)

### 2. 研报提炼(6/17 → 10/17)
- 第3批4篇入库:长江(已有)、天风债基本土化、NAFMII四维、华宝净值Campisi全文、晨星
- 债基归因/评价方法论**齐了**,直接喂给了下面的 v0.4 框架
- 产出3条 v0.4 可操作要点(净值+持仓双轨、CPR持续性、信用风险三件套)

### 3. 固收线 v0.4 框架文档(新)★
- `10_固收线框架v0.4.md`:把6篇债基研报落成可实施设计
- 核心:净值五因子模型(华宝口径,复用rbsa求解器)、纯债/固收+两套记分卡、债基L0-L2流水线、且慢债基工具映射、实施顺序
- 设计铁律:票息不计能力分只作标签;净值法+持仓法双轨;纯债vs含权分流
- **不动主线代码**,待主动权益v0.3收口后实施

### 4. E1 费率源调研+接入
- 调研确认:AKShare `fund_info_ths`(同花顺源)含管理费/托管费
- 已接入 `data_akshare.fund_meta`:取管理费+托管费 → E1 total_fee,失败降级
- E 维现有 E1费率 + E2换手两个指标
- ⚠️ **待本机实测核对字段名**(沙箱无 akshare 网络);⚠️ 每只额外一次调用,全量会增耗时,代码注释里写了"若拖慢可改批量缓存"

## 回来后请做

```powershell
cd D:\03_AI_Projects_and_Vault\Fund_Evaluation\scripts
.venv\Scripts\activate
python test_engine.py        # 确认仍全过(本夜未改scoring/test, 应通过)
# 验证E1费率字段(单只快速测):
python -c "import akshare as ak; print(ak.fund_info_ths(symbol='000979'))"
# 看输出里管理费/托管费的实际字段名, 若与'管理费'/'托管费'不符, 改 data_akshare.py 第215-216行
cd ..
.\run.ps1 sync "feat(自主): C维+9只 + 研报10/17 + v0.4固收框架 + E1费率接入"
```

## 待决策/提醒
- E1 费率字段名需本机实测核对(见上)
- v0.4 固收线待主动权益 v0.3 收口(C维数据齐、formal成主流)后启动
- 研报剩 7/17(FOF/经理研究类 + 2篇待下载PDF),定时任务每天12:30继续

## 本夜未碰
- scoring.py / run_monthly.py 主评分逻辑(E1只在data层加取数,scoring裸列名自动接)
- 已投产的 v0.3 流水线
