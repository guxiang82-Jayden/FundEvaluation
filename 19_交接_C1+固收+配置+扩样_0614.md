# 交接 · C1选股占比 + 固收+配置 + C维扩样(2026-06-14, Opus 4.8)

> 承 16/17。本批三件:① C1 选股占比(净值法,补满纯债 C 维)② 固收+ 记分卡配置(主线备好,模块派给 Codex)③ C 维数据扩样到 87 只。

## 一、改动(主线)
- `scripts/metrics_bond.py`:新增 `_selection_share()`,由 Campisi 回归 alpha vs 因子β贡献衍生 `selection_share_bond`(C1,净值法代理 NAFMII"择券占超额比",无需持仓)。compute_bond_metrics 现输出 C1。
  - **意义**:纯债 C 维现在**仅凭净值即可全维 formal**(C1净值法 + C2/C3/C4持仓);命中 cdim_bond 的基金再叠加 C2/C3/C4 增强。
- `scripts/config.py`:新增 `BOND_PLUS_DIM_WEIGHTS`(A25/B40/C20/D10/E5)、`BOND_PLUS_INDICATORS`、`BOND_PLUS_EQUITY_BANDS`(保守/稳健/积极)、`BOND_PLUS_TARGET_DD=-0.03`。
- `scripts/test_bond_pipeline.py`:断言更新(C 维现由净值算出 → 覆盖率 1.0 / ABCDE / formal)。

## 二、数据(scripts/data/)
- `cdim_bond_data.csv`:**69 → 87 只**(新增 18:纯债中大盘 + 中欧丰利/南方宝元二级债 + 5 只偏债混合)。credit_sink 87/87,duration 76/87(11 只高杠杆/短债且慢未给久期,C 维按可用指标归一)。
- `bond_universe_seed.csv`:同步 87 只。

## 三、派给 Codex 的子任务
- `18_子任务_固收+记分卡_给Codex.md`:新建 `scoring_bond_plus.py` + 测试。
  - 主线已备好 config.BOND_PLUS_* 与参数化 scoring.score_all,Codex 只需:权益中枢分组 + 两个固收+特有指标(target_dd_pass 目标回撤达标率 / equity_contrib_ratio 股债贡献分解,均净值法可算)+ 组装评分。
  - 隔离:只新建文件,不改任何现有 .py,本地 commit 不 push。

## 四、验证(沙箱)
- test_engine(主动权益)/ test_bond_pipeline(债基端到端,现 ABCDE→formal)/ test_cdim_bond(87只命中)全过 ✅

## 五、回来后请做(本机)
```powershell
cd D:\03_AI_Projects_and_Vault\Fund_Evaluation\scripts ; .venv\Scripts\activate
python test_engine.py ; python test_bond_pipeline.py ; python test_cdim_bond.py
python run_monthly_bond.py --limit 40   # C维(净值C1 + 命中持仓C2-4)生效
# Codex 完成 scoring_bond_plus 后, 跑其 test_scoring_bond_plus.py
cd .. ; .\run.ps1 sync "feat(v0.4): C1选股占比 + 固收+配置 + C维扩样87 + Codex固收+任务包"
```

## 六、下一步
1. 收 Codex 的 scoring_bond_plus,接入 run_monthly_bond(固收+ 分支:偏债混合/二级债走 BOND_PLUS)
2. 真实主榜跑出后对未覆盖基金继续扩样(同脚本)
3. C2 信用下沉精化:若且慢评级分布接口后续有数,替换 creditAssetRatio 代理
