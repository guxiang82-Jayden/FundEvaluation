# 交接 · 固收+ 分流接入 + Codex 模块对接(2026-06-14, Opus 4.8)

> 承 19。Codex 已交付 `scoring_bond_plus.py`(权益中枢分组记分卡),本批把它接进 `run_monthly_bond.py` 的分流骨架,债基线现为**双 track**。

## 一、改动(主线)
- `scripts/run_monthly_bond.py`:新增固收+ 分流骨架
  - `split_tracks()`:按 `bond_subgroup` 分流 —— `{偏债混合/固收+, 混合债券二级}` 走 **BOND_PLUS**,其余(纯债/混合一级/可转债)走 **BOND**。
  - `score_bond_track()`:纯债 BOND 记分卡(子组内,<10只回退"纯债综合")。
  - `score_plus_track()`:优先调 Codex 的 `scoring_bond_plus.score_bond_plus`(先 `build_bond_plus_metrics` 用净值+权益指数补 target_dd_pass/equity_contrib_ratio,再按权益档评分);**软依赖**——模块缺失或缺 equity_position 时自动用纯债记分卡兜底(scorecard=BOND(fallback))。
  - `build_bond_metrics_table()` 现返回 `(df, navs)`,navs 供固收+ 的股债贡献/达标率复用。
  - main 取权益指数(默认基准)传入;输出新增 `scorecard` 列(BOND / BOND_PLUS)。

## 二、Codex 交付(已验收)
- `scripts/scoring_bond_plus.py` + `scripts/test_scoring_bond_plus.py`:
  - `equity_center_band`(保守<10%/稳健10-20%/积极20-30%)、`target_dd_pass`(60日滚动回撤达标率)、`equity_contrib_ratio`(净值对权益指数OLS分解)、`build_bond_plus_metrics`、`score_bond_plus`(同档内 BOND_PLUS 评分,未分组/小样本 defer)。
  - Codex 自测通过;主线对接测试通过。

## 三、验证(沙箱)
- 新增 `scripts/test_bond_tracks.py`:分流→双 track 评分,固收+ 走 BOND_PLUS(档位 稳健/积极)✓
- `test_scoring_bond_plus.py`(Codex)✓ / test_engine ✓ / test_bond_pipeline ✓ / test_cdim_bond ✓

## 四、⚠️ 重要:本机文件完整性
本会话挂载层再次出现**文件被异步截断**现象,发现并从 git HEAD 还原了两处工作树副本:
- `scripts/data_akshare.py`(被截到 260 行)→ 还原为 291 行(HEAD 完整版)
- `scripts/run_monthly.py`(被截到 149 行)→ 还原为 161 行(HEAD 完整版)
**你已推送的 d8201bb 是完整的**,远端无损;仅工作树副本曾被截断,现已还原。提交前建议扫一眼 `git diff --stat`,确认无意外的大删减。

## 五、回来后请做(本机)
```powershell
cd D:\03_AI_Projects_and_Vault\Fund_Evaluation\scripts ; .venv\Scripts\activate
python test_engine.py ; python test_bond_pipeline.py ; python test_cdim_bond.py
python test_bond_tracks.py ; python test_scoring_bond_plus.py
python run_monthly_bond.py --limit 60   # 双track真跑(纯债BOND + 固收+BOND_PLUS)
cd .. ; .\run.ps1 sync "feat(v0.4): 固收+分流接入 + Codex scoring_bond_plus对接 + 分流测试"
```

## 六、下一步
1. 真实主榜跑出后:固收+ 各档样本量确认(<5只档位会 defer 不评分,需扩样)
2. 输出 Excel 增加"固收+榜"sheet(按 scorecard 分表)
3. v1.0 复盘机制 review.py 纳入债基双 track
