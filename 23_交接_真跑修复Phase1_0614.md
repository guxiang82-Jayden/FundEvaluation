# 交接 · 债基真跑质检 + Phase1 口径修复(2026-06-14, Opus 4.8)

> 承 Codex 真跑质检报告 `scripts/data/bond_run_report.md`(基线 cf7ee67)。本批据报告修 P0/P2,使评分 universe 干净、分组口径可信。

## 一、Codex 真跑暴露的问题(已采纳)
1. 份额未合并:16组重复份额/32行,污染分位与TopN(P0)
2. 可转债(R²0.03)、混合一级(R²0.09)塞进"纯债"track,拉垮解释力(P0)
3. 小组<10回退到同一"纯债综合",短债与可转债异质合并失真(P0)
4. 筛选 FN3-FN6 真实覆盖极低,"通过初筛"言过其实(P1)
5. 指数型-固收(673)/QDII(83) 混入,口径不符(P2)
6. test_bond_tracks 写死 Linux 路径,Windows 假失败(P2,主线遗留)

## 二、本批修复(`run_monthly_bond.py` 重写 + 用户拍板)
- **份额合并**:`bond_universe` 复用 `da.merge_share_classes`,一产品一行。
- **4-Track**(用户定):`assign_track` 按 fund_type+subgroup 路由
  - BOND 纯债(短期/中长期,**组内分位**)/ BOND1 一级债 / PLUS 固收+(权益档,scoring_bond_plus)
  - CB 可转债、BOND_INDEX 指数固收/QDII → **单列"待专用卡",暂不评分**
- **小组 defer 不跨异质**:`score_subgroups` 组内<5 直接 defer,不再回退合并。
- **universe**(用户定):纳入 指数型-固收/QDII 但打标 BOND_INDEX 单列;**先排除同业存单**。
- **筛选 coverage**:`screening_coverage` 输出各 FN 字段非空率(字段存在但大量空 ≠ 风险已查)。
- **Excel 多表**:纯债主榜/一级债榜/固收+榜/可转债待评/工具型待评/小微观察区/剔除清单。
- **修测试路径**:`test_bond_tracks.py` 改用 `tempfile`。

## 三、验证(沙箱合成)
test_bond_tracks(assign_track/组内defer/BOND_PLUS/多表)、test_engine、test_bond_pipeline、test_cdim_bond、test_scoring_bond_plus 全过。

## 四、回来后请做(本机)
```powershell
cd D:\03_AI_Projects_and_Vault\Fund_Evaluation\scripts ; .venv\Scripts\activate
python test_bond_tracks.py ; python test_engine.py ; python test_bond_pipeline.py
python run_monthly_bond.py --limit 80   # 看4-Track分布 + 各榜 + 待评sheet
cd .. ; .\run.ps1 sync "feat(v0.4): 真跑修复 份额合并+4Track+小组defer+universe单列BOND_INDEX + Codex质检报告"
```

## 五、Phase 2 待办(可再派 Codex/mac)
- **可转债专用记分卡**(CB):补转债/权益因子,或不展示可解释为择券的 alpha。
- **BOND_INDEX 工具型记分卡**:跟踪误差/费率/规模/流动性/指数代表性(不用 Campisi/经理分)。
- **Campisi 按子组配因子**:短债用更短期限 level/slope 代理;一级债考虑转债暴露。
- **维度覆盖率 vs 指标覆盖率分离**:formal 要求关键持仓指标齐全,C 维单独标命中。
- **份额合并求和规模**:当前只保留主份额规模(可能误触 FN1)。
- **review.py 债基双track复盘**(任务16):现已有真实 archive,可启动。
- 真跑改分层抽样(子组/权益档/C维命中/成立年限)。

## 六、并行:mac 回测分支
`mac/backtest-calib` 为孤立历史(unrelated histories),不能常规 merge;用 `git checkout FETCH_HEAD -- <backtest文件>` 抽取到工作区评审,再作普通 commit 落 main(署 mac)。审阅中。
