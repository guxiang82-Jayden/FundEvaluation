# 交接 · review.py 双线复盘升级(2026-06-14 夜, 自主批次)

> 你休息/跑回测期间完成。不改任何被 run_backtest 引用的文件(scoring/metrics/config/data_akshare 均未动),与你的回测跑批零冲突。

## 完成(任务16)
`scripts/review.py` 从权益骨架升级为**权益+债基双线**复盘:
- **双线 load**:`load_archives(kind="equity"|"bond")`;债基自动合并 纯债主榜/一级债榜/固收+榜 多 sheet,按 fund_code 去重。
- **真实前瞻收益**:`forward_returns_from_nav(codes, start, end, nav_func)` 算两期区间净值收益;优先级 `forward_returns 传入 > nav_func 计算 > 下期存档代理(标注不可靠)`。CLI `--real-nav` 用 akshare 真净值。
- **复盘四问**:
  - Q1 命中率(`hit_rate`,Top20% 跑赢同类中位,同类列自适应 effective_group/bond_subgroup/scorecard)
  - Q3 各维 RankIC(`rank_ic`)
  - Q4 可比性break(`comparability_breaks`,规模突变≥2x/换将 且标注是否上期Top高分)
  - Q2 规则有效性(`rule_effectiveness`,兼容 N*/FN*;待接真实剔除清单+前瞻)
- `run_review(kind=, forward_returns=, nav_func=)` 产出 `复盘报告_{kind}_{prevd}_to_{curd}.md`。
- 新增 `scripts/test_review.py`:合成 archive 全覆盖(命中率/RankIC/break/债基多榜合并/端到端),通过。

## 验证
test_review + test_engine/test_bond_pipeline/test_cdim_bond/test_bond_tracks/test_scoring_bond_plus 全过。

## 用法(本机, ≥2期归档后)
```powershell
# 债基(需先有≥2期 archive/score_bond_*.xlsx 全量跑批归档)
python review.py --kind bond --real-nav
# 权益
python review.py --kind equity --real-nav
```
注:`run_monthly_bond.py --limit` 不归档;季度全量跑批才写 archive/,积累 2 期后复盘才有意义。

## 现状/下一步
- v1.0 闭环三件:回测(框架就位,你正在跑证据)/ 复盘(本批框架就位)/ 深研L3(未启)。
- Phase2 待办仍在 23 号交接:可转债CB专用卡(见 25 号子任务包)、BOND_INDEX工具型卡、Campisi子组因子、份额合并求和规模、维度/指标覆盖率分离。
- 我未擅自把可转债/工具型方法学塞进实时流水线(涉及评价口径决策),改为打包成子任务待你过目后派发。
